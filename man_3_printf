.TH _printf
.SH NAME
_printf - formatted output conversion
.SH SYNOPSIS
        int _printf(const char *format, ...);
.SH DESCRIPTION
The _printf() function writes output to stdout according to a format string. The 'format' is a null-terminated string that contains zero or more directives.
.SH OPTIONS
Format specifiers (preceded by a '%' sign in the format string):
.IP \%c
Print a single character.
.IP \%s
Print a string of characters.
.IP \%d or \%i
Print an integer.
.IP \%\%
Print a percent sign.
.SH RETURN VALUE
Upon successful return, _printf() returns the number of characters printed (excluding the null byte used to end output to strings).
.SH ERRORS
No known errors.
.SH EXAMPLES
        _printf("Hello, World!\\n");
Prints "Hello, World!" followed by a newline to stdout.
        _printf("Number: \%d\\n", 5);
Prints "Number: 5" followed by a newline to stdout.
.SH BUGS
No known bugs.
.SH AUTHOR
Arbes Avdiaj and Ralf Semi.
